import React from 'react'

const TestingComponent = ({ movieData }) => {

    // console.log(movieData.netflixOriginals);
    // console.log(movieData.trendingNow);
    // console.log(typeof movieData.topRated);
    return (
        <div>

        </div>
    )
}

export default TestingComponent
